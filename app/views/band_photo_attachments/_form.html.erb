<%= form_for(@band_photo_attachment) do |f| %>
  <% if @band_photo_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@band_photo_attachment.errors.count, "error") %> prohibited this band_photo_attachment from being saved:</h2>

      <ul>
      <% @band_photo_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :band_id %><br>
    <%= f.number_field :band_id %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <%= f.text_field :photo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
