<h1>Bands#show</h1>
<h3><%= @band.name %></h3>

<% @slots.each do |slot| %>
  <% @events.each do |event| %>
    <% if slot.event_id == event.id %>
      <h4> <%= link_to event.title, event %></h4>
      <p> <%= slot.time %></p>
    <% end %>
  <% end %>
<% end %>